<!doctype html>
<html lang="en">
    <!--<head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script>
            var params = new URLSearchParams(window.location.search);
            var base = params.get("base") || "";
            if (base) {
                var tag = document.createElement("base");
                tag.href = base;
                document.head.appendChild(tag);
            }
        </script>
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            html,
            body {
                width: 100%;
                height: 100%;
                overflow: hidden;
                background: #000;
            }
            canvas {
                display: block;
            }
        </style>
    </head>
    <body>
        <script>
            (function () {
                var params = new URLSearchParams(window.location.search);

                var base = params.get("base") || "";
                var sketch = params.get("sketch") || "";
                var files = params.get("files") || "";
                var sound = params.get("sound") === "1";

                // Derive site root from loader's own URL
                // loader lives at /p5loader.html so strip the filename
                var site_root =
                    window.location.origin +
                    window.location.pathname.replace(/\/[^/]*$/, "/");

                function load_script(src, on_load) {
                    var s = document.createElement("script");
                    s.src = src;
                    if (on_load) s.onload = on_load;
                    // document.head.appendChild(s);
                    document.body.appendChild(s);
                }

                function load_sketches() {
                    console.log("base:", base);
                    console.log("sketch:", sketch);
                    console.log("files:", files);
                    var lib_list = files
                        ? files
                              .split(",")
                              .map(function (l) {
                                  return l.trim();
                              })
                              .filter(Boolean)
                        : [];

                    function load_next(i) {
                        if (i < lib_list.length) {
                            load_script(base + lib_list[i], function () {
                                load_next(i + 1);
                            });
                        } else {
                            if (sketch) load_script(base + sketch);
                        }
                    }
                    load_next(0);
                }

                load_script(site_root + "js/p5.min.js", function () {
                    if (sound) {
                        load_script(
                            site_root + "js/p5.sound.min.js",
                            load_sketches,
                        );
                    } else {
                        load_sketches();
                    }
                });
            })();
        </script>
    </body>-->
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script>
            var params = new URLSearchParams(window.location.search);
            var base = params.get("base") || "";
            if (base) {
                var tag = document.createElement("base");
                tag.href = base;
                document.head.appendChild(tag);
            }
        </script>
        <script src="/js/p5.min.js"></script>
        <!--<script src="/js/p5.sound.min.js"></script>-->
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            html,
            body {
                width: 100%;
                height: 100%;
                overflow: hidden;
                background: #000;
            }
            canvas {
                display: block;
            }
        </style>
    </head>
    <body>
        <script>
            (function () {
                var params = new URLSearchParams(window.location.search);
                var base = params.get("base") || "";
                var sketch = params.get("sketch") || "";
                var files = params.get("files") || "";

                function load_script(src, on_load) {
                    var s = document.createElement("script");
                    s.src = src;
                    if (on_load) s.onload = on_load;
                    document.body.appendChild(s);
                }

                var file_list = files
                    ? files
                          .split(",")
                          .map(function (l) {
                              return l.trim();
                          })
                          .filter(Boolean)
                    : [];

                function load_next(i) {
                    if (i < file_list.length) {
                        load_script(base + file_list[i], function () {
                            load_next(i + 1);
                        });
                    } else {
                        if (sketch) load_script(base + sketch);
                    }
                }
                load_next(0);
            })();
        </script>
    </body>
</html>
